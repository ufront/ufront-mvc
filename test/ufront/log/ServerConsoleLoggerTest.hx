package ufront.log;

import utest.Assert;
import ufront.log.ServerConsoleLogger;
import ufront.log.Message;
import ufront.app.UFLogHandler;
using ufront.test.TestUtils;

class ServerConsoleLoggerTest {
	public function new() {}

	// Nothing to test really, it is all platform specific and the effects aren't unit testable.
}
